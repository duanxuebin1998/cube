# 传感器安全通信协议 V2 精简候选版

文档状态：待评审、未实现、未形成认证证据。

线上候选版本：`protocol_version=0x02`。

文档修订：2026-08-07 精简设计。
适用目标：CPU2 可连接 1～2 个不同功能的安全相关传感器，但任一时刻最多只有一个传感器主动上报。首个已定义 Runtime Profile 为 DM4；另一传感器可能是电导率、含水率或其它尚未设计的传感器。

> 本文只说明按 SIL2 安全通信原则设计的候选协议。协议设计、代码、测试或构建均不能单独证明整机已经满足 SIL2/SC3。

## 1. 一页结论

V2 精简版把安全运行核心收敛为三条控制命令和一种运行帧：

```text
HELLO
START_STREAM
ACTIVE_REPORT
STOP_STREAM
```

核心行为如下：

1. 控制阶段双向通信；安全运行阶段 CPU2 完全静默，只接收唯一活动传感器的周期上报。
2. `HELLO` 建立新的 64 位会话，并安全终止该端点可能残留的旧主动流。
3. `START_STREAM` 同时完成 Profile 选择、配置核对和启动授权，不再拆成 CONFIGURE 与 ARM 两步。
4. 周期、允许抖动、启动延时、停止窗口和最大年龄由静态 Profile 契约确定，不在每次连接时动态协商。
5. 运行帧保留地址、类型、Profile、会话、序号、采样计数、数据年龄、配置签名、状态、诊断、有效字段和 CRC32C。
6. 运行公共头由旧草案的 52 字节缩减为 44 字节；DM4 运行帧由 100 字节缩减为 92 字节。
7. 参数维护不属于安全运行核心；任何影响安全输出的参数提交都必须终止会话、更新摘要并重新 HELLO。

精简不是删除安全机制，而是删除重复身份、动态组合和中间状态，降低 SC3 需求追踪、代码分支和测试组合数。

## 2. 已确认输入和开放项

### 2.1 用户已确认

- 两个端点是不同功能传感器，不是两个相同设备的复制。
- 第二传感器尚未设计，可能为电导率、含水率或其它传感器。
- V2 架构必须支持后续增加新传感器类型。
- 安全模式采用传感器主动上报；运行阶段 CPU2 单向接收。
- 不会存在两个传感器同时开启主动上报模式。
- DM4 业务字段继续使用《多参数传感器通信协议 V4.0》的参数语义。

### 2.2 本版冻结的结构性决定

| 项目 | V2 精简规则 |
| --- | --- |
| 已配置端点数 | 1～2 |
| 同时活动端点数 | 0 或 1 |
| 物理层基线 | 9600 bps、8N1、半双工 RS485 |
| 控制命令 | `HELLO`、`START_STREAM`、`STOP_STREAM` |
| 控制帧最大长度 | 64 字节 |
| 运行帧最大长度 | 120 字节，44 字节公共头＋最多 72 字节 Profile＋4 字节 CRC |
| 完整性校验 | CRC32C Castagnoli |
| Profile 选择 | 编译期静态注册，线上只发送 ID、版本和安全功能 ID |
| 会话 | 每次 HELLO 建立新的 64 位 `session_id`，一次 START 只使用一次 |
| 运行期应答 | 无 ACK、无查询、无同步、无重发请求 |

### 2.3 尚待冻结

- 整机安全功能 FTTI、超时后的具体整机安全状态。
- 第二传感器的 `sensor_type`、Profile 字段、单位、倍率、范围和诊断。
- 两个端点的生产地址、设备身份分配和可选/必需角色。
- DM4 正式 `profile_contract_crc32`、安全参数清单和正式参数摘要。
- DM4 各安全功能最终质量阈值、变化率范围和拒绝诊断掩码。
- 真实传感器、目标线长、最坏环境下的时序和误码证据。

这些开放项不改变公共状态机和帧结构，但必须在生成机器契约和认证证据前关闭。

## 3. 相对旧 V2 草案的精简

| 旧草案 | 精简版 | 安全影响 |
| --- | --- | --- |
| `CONFIGURE_ACTIVE_UPLINK`＋`ARM_ACTIVE_UPLINK` | 合并为 `START_STREAM` | 仍要求响应完整回显并在响应后延时启动 |
| `GET_ACTIVE_UPLINK` | 删除 | HELLO 返回端点状态；运行期禁止查询 |
| `bus_session_id`＋`sensor_session_id`＋`stream_id`＋`uplink_epoch` | 单个 64 位 `session_id` | 同时包含 CPU2 和传感器新鲜因子，禁止复用 |
| 动态协商 7 个时序参数 | 静态 Profile 契约 | 减少参数组合和双方理解偏差 |
| Profile 五元组在线协商 | 静态注册表＋`profile_catalog_crc32` | 未登记组合仍拒绝，降低运行解析复杂度 |
| `config_epoch`＋多个配置字段 | `profile_config_crc32`＋`safety_param_crc32` | 分别绑定协议配置和实际安全参数 |
| 32 位状态＋32 位诊断 | 16 位状态＋16 位诊断 | 当前定义只使用 7 位和 14 位，保留位仍强制为 0 |
| 52 字节运行公共头 | 44 字节运行公共头 | 保留显式源、目的、类型和全部必要安全门禁 |

由于旧 V2 草案从未实现或冻结，本次仍使用 `protocol_version=0x02`。若确认已有外部样机按旧草案实现，必须将精简版提升为新的协议版本，不能依靠帧长差异隐式兼容。

## 4. 安全不变量

以下规则是协议不变量，不得由项目配置关闭：

1. 任一时刻 `active_endpoint_count <= 1`。
2. 只有 CPU2 可以发起控制事务；不使用广播 START。
3. 同一条总线任一时刻最多一个未完成控制事务。
4. CPU2 只有在完整合法的 `START_STREAM_RSP` 后才能进入 `RX_ONLY_ACTIVE`。
5. `RX_ONLY_ACTIVE` 期间 CPU2 不发送任何查询、ACK、同步或维护命令。
6. 活动传感器每个周期最多发送一帧最新完整快照，不重发旧快照。
7. 任一帧门禁失败都不得更新安全快照，不采用部分字段。
8. STOP 或 HELLO 重建后，旧 `session_id` 永久失效。
9. 参数、Profile、时序或安全功能配置变化后，旧会话和旧配置签名立即失效。
10. 旧端点未停止并确认静默前，CPU2 不得启动另一个端点。
11. 收到非活动端点的合法运行帧属于总线级互斥故障。
12. 恢复后必须使用新会话、新鲜样本和重新核对的配置签名，禁止恢复故障前缓存。

## 5. 节点、地址和传感器类型

```text
                         半双工 RS485
CPU2 0x01  ────────────────┬────────────────────
                            │
                    端点 A 0x10
                            │
                    端点 B 0x11

安全运行时 A 或 B 至多一个发送
```

| 地址 | 对象 | 候选规则 |
| --- | --- | --- |
| `0x01` | CPU2 | 唯一控制发起方和安全数据接收方 |
| `0x10` | 端点 A | 候选生产地址 |
| `0x11` | 端点 B | 候选生产地址，必须与 A 不同 |
| `0xFF` | 广播 | V2 安全核心禁止广播 START |
| `0x00` | 未分配 | 不继承非安全协议“0 为广播”的语义 |

传感器类型候选分配：

| `sensor_type` | 含义 | 状态 |
| --- | --- | --- |
| `0x00` | 无效/未配置 | 禁止建立安全服务 |
| `0x01` | DM4 | 候选分配，待评审冻结 |
| `0x02`～`0xEF` | 后续正式传感器类型 | 受控注册，不得复用 |
| `0xF0`～`0xFF` | 保留 | V2 初版拒绝 |

CPU2 为每个端点静态配置期望地址、`sensor_type`、`device_id`、允许 Profile 和 `profile_catalog_crc32`。运行帧仍显式携带源地址、目的地址和传感器类型，避免依赖线路拓扑猜测身份。

## 6. 静态 Profile 注册

### 6.1 注册键

安全运行 Profile 使用以下静态键：

```text
(sensor_type, profile_id, profile_version, safety_function_id)
```

- `profile_id=0x00` 和 `safety_function_id=0x00` 无效。
- 固件编译时冻结负载长度、字段偏移、类型、倍率、范围、必需字段和时序。
- 运行期不使用 TLV、可选字段、可变数组或动态字段目录。
- 未登记组合必须拒绝，不能根据相同帧长猜测解析。
- 字段语义、类型、signedness、倍率或有效位发生不兼容变化时提升 Profile 版本。

### 6.2 新 Profile 最低准入内容

每个新 Profile 至少冻结：

1. 静态注册键和固定负载长度。
2. 字段偏移、类型、单位、倍率、范围、保留值和无效值。
3. `field_valid_mask` 位映射和各安全功能的必需字段掩码。
4. 周期、允许抖动、启动延时、停止窗口、静默确认和最大年龄。
5. 状态、诊断、自检、质量和变化率对字段有效性的影响矩阵。
6. 进入 `safety_param_crc32` 的参数 ID 清单和规范化编码。
7. 正常、边界、错误、重启和 golden frame 测试向量。

Profile 负载超过 72 字节、有效字段组超过 16 个或一个周期需要多帧时，必须评审新的协议版本，不能私自扩展 V2。

## 7. 状态机

### 7.1 CPU2 状态

```text
OFFLINE
  │ HELLO，核对身份、目录和新会话
  ▼
CONTROL_READY
  │ START_STREAM，完整验证响应
  ▼
RX_ONLY_ACTIVE
  │ CPU2完全静默，唯一端点周期上报
  │
  ├─ 正常停止 ───────> EXITING_RX_ONLY ── STOP/静默确认 ──> OFFLINE
  ├─ 端点/数据错误 ──> ENDPOINT_RECOVERING ── HELLO重建 ──> CONTROL_READY
  └─ 错源/碰撞/占线 ─> BUS_RECOVERING
```

`STOP_STREAM` 成功并确认静默后，当前会话失效。下一次测量必须重新 HELLO，不复用旧会话。

### 7.2 传感器状态

| 状态 | 行为 |
| --- | --- |
| `INACTIVE_SILENT` | 默认状态，不主动发送，只接受本地址控制帧 |
| `CONTROL_READY` | HELLO 已建立新会话，可接受一次 START |
| `START_DELAY` | START 响应已经发送，等待 Profile 固定启动延时 |
| `UPLINK_ACTIVE` | 按本地周期发送一帧新安全快照 |
| `FAULT_SILENT` | 会话、配置或内部安全错误，停止发送 |

传感器重启后必须进入 `INACTIVE_SILENT`，不得从非易失参数自动恢复主动上报。

### 7.3 HELLO 的安全停止语义

完整合法且寻址正确的 HELLO 可以在 `INACTIVE_SILENT`、`CONTROL_READY`、`UPLINK_ACTIVE` 或 `FAULT_SILENT` 状态处理：

1. 取消尚未发送的后续运行帧。
2. 使旧会话失效。
3. 生成新的 64 位 `session_id`。
4. 返回 HELLO 响应并进入 `CONTROL_READY`。

因此 CPU2 重启或未知旧流恢复时不需要知道旧会话即可安全停止指定端点。HELLO 只能停止服务，不会在没有 START 的情况下启动发送；随机通信错误仍由地址、长度、字段约束和 CRC32C 检出。

## 8. 公共编码和 CRC

| 项 | V2 规则 |
| --- | --- |
| 字节序 | 所有多字节整数小端 |
| 运行测量编码 | 固定宽度整数和明确倍率，不直接发送 IEEE 754 浮点 |
| 完整性校验 | CRC32C Castagnoli |
| 反射实现多项式 | `0x82F63B78` |
| 初值/结果异或 | `0xFFFFFFFF` / `0xFFFFFFFF` |
| CRC 输出顺序 | 小端 |
| 保留字段 | 发送为 0；接收路径遇到非 0 时拒绝 |

控制帧和运行帧的 CRC32C 均从偏移 2 的 `frame_type` 覆盖到负载最后一个字节，不覆盖两个 SOF 字节和 CRC 字段自身。

CRC32C 用于随机通信错误检测，不是密码学认证。若威胁模型包含主动伪造，必须增加认证、访问控制或物理隔离。

## 9. 帧类型

| 帧 | `frame_type` | 方向 | 长度 | 用途 |
| --- | ---: | --- | ---: | --- |
| `CTRL_REQ_V2` | `0xC1` | CPU2 -> 单端点 | 24～64 字节 | HELLO、START、STOP 请求 |
| `CTRL_RSP_V2` | `0xC2` | 单端点 -> CPU2 | 24～64 字节 | 对应控制响应 |
| `ACTIVE_REPORT_V2` | `0xA2` | 活动传感器 -> CPU2 | 48～120 字节 | 运行期唯一主动上报帧 |

V2 不定义同步帧、双时隙帧、数据 ACK 或运行期参数查询。V1 的 `FAST_REPORT_FRAME(frame_type=0xA1)` 不得作为 V2 安全输入。

## 10. 控制帧

### 10.1 20 字节控制头

| 偏移 | 字段 | 长度 | 类型 | 说明 |
| ---: | --- | ---: | --- | --- |
| 0 | `sof0` | 1 | u8 | 固定 `0xA5` |
| 1 | `sof1` | 1 | u8 | 固定 `0x5A` |
| 2 | `frame_type` | 1 | u8 | 请求 `0xC1`，响应 `0xC2` |
| 3 | `frame_len` | 1 | u8 | `20 + payload_len + 4` |
| 4 | `src_id` | 1 | u8 | 发送方地址 |
| 5 | `dst_id` | 1 | u8 | 被寻址端点 |
| 6 | `protocol_version` | 1 | u8 | 固定 `0x02` |
| 7 | `command` | 1 | u8 | 命令值 |
| 8 | `transaction_seq` | 2 | u16 | 请求非 0，响应原样回显 |
| 10 | `result_code` | 1 | u8 | 请求固定 0，响应给出结果 |
| 11 | `reserved0` | 1 | u8 | 固定 0 |
| 12 | `session_id` | 8 | u64 | HELLO 请求固定 0，其它控制绑定当前会话 |
| 20 | `payload` | N | bytes | 由命令和方向确定固定长度 |
| 20+N | `frame_crc32c` | 4 | u32 | CRC32C |

接收端通过 `frame_len - 24` 得到负载长度，并必须与命令定义的固定长度完全一致。禁止忽略尾部多余字节或接受截断负载。

### 10.2 控制命令

| 命令 | 值 | 请求负载 | 成功响应负载 | 作用 |
| --- | ---: | ---: | ---: | --- |
| `HELLO` | `0x01` | 8 字节 | 20 字节 | 安全停止旧流、核对身份并建立新会话 |
| `START_STREAM` | `0x20` | 12 字节 | 12 字节 | 核对 Profile 和配置后启动唯一流 |
| `STOP_STREAM` | `0x21` | 0 字节 | 0 字节 | 停止当前流并使会话失效 |

错误响应负载固定为 0 字节。错误响应仍回显命令、事务序号和可用的当前会话；CPU2 不得因错误响应更新控制状态。

### 10.3 结果码

| 值 | 名称 | 含义 |
| ---: | --- | --- |
| `0x00` | `OK` | 请求完整执行 |
| `0x01` | `UNSUPPORTED_VERSION` | 不支持协议版本 |
| `0x02` | `UNKNOWN_COMMAND` | 未定义命令 |
| `0x03` | `INVALID_LENGTH` | 固定长度不匹配 |
| `0x04` | `INVALID_STATE` | 当前状态不允许执行 |
| `0x05` | `SESSION_MISMATCH` | 会话不匹配或已失效 |
| `0x06` | `PROFILE_MISMATCH` | Profile、功能或目录不匹配 |
| `0x07` | `CONFIG_MISMATCH` | Profile 配置或安全参数摘要不匹配 |
| `0x08` | `IDENTITY_MISMATCH` | 地址、类型或设备身份不匹配 |
| `0x09` | `BUSY` | 已有未完成控制事务 |
| `0x0A` | `INTERNAL_FAULT` | 传感器内部安全故障 |
| `0x0B` | `RESERVED_NONZERO` | 保留字段或保留位非 0 |
| `0x0C`～`0xFF` | 保留 | V2 初版拒绝 |

SOF、地址、最小长度或 CRC 错误的帧不返回响应，避免对噪声和错址帧产生总线发送。

## 11. HELLO

### 11.1 HELLO 请求负载

| 负载偏移 | 字段 | 长度 | 类型 | 规则 |
| ---: | --- | ---: | --- | --- |
| 0 | `payload_version` | 1 | u8 | 固定 1 |
| 1 | `expected_sensor_type` | 1 | u8 | CPU2 静态配置的期望类型，不使用通配值 |
| 2 | `reserved0` | 2 | u16 | 固定 0 |
| 4 | `host_nonce` | 4 | u32 | CPU2 本次 HELLO 新鲜因子，非 0 |

HELLO 控制头中的 `session_id` 固定为 0。

### 11.2 HELLO 成功响应负载

| 负载偏移 | 字段 | 长度 | 类型 | 规则 |
| ---: | --- | ---: | --- | --- |
| 0 | `payload_version` | 1 | u8 | 固定 1 |
| 1 | `sensor_type` | 1 | u8 | 端点正式类型 |
| 2 | `sensor_state` | 1 | u8 | 成功后固定 `CONTROL_READY=0x01` |
| 3 | `reserved0` | 1 | u8 | 固定 0 |
| 4 | `device_id` | 8 | u64 | 生产唯一身份，非 0 |
| 12 | `sensor_boot_counter` | 4 | u32 | 传感器启动身份或等价单调值 |
| 16 | `profile_catalog_crc32` | 4 | u32 | 当前编译期 Profile 目录摘要 |

HELLO 响应头携带新 `session_id`：

```text
session_id[63:32] = host_nonce
session_id[31:0]  = sensor_nonce
```

`sensor_nonce` 必须非 0，并且在同一传感器启动周期内每次 HELLO 都不同。传感器应将启动计数、会话计数和本地新鲜源纳入生成过程。CPU2 必须检查高 32 位等于本次 `host_nonce`、低 32 位非 0，且完整 64 位不等于保留的近期会话。

`profile_catalog_crc32` 不用于动态下载字段表，只用于确认 CPU2 和传感器使用同一份静态 Profile 契约。

## 12. START_STREAM

### 12.1 请求和成功响应负载

请求与成功响应使用同一 12 字节负载，响应必须逐字节回显：

| 负载偏移 | 字段 | 长度 | 类型 | 规则 |
| ---: | --- | ---: | --- | --- |
| 0 | `payload_version` | 1 | u8 | 固定 1 |
| 1 | `profile_id` | 1 | u8 | 静态登记 Profile |
| 2 | `profile_version` | 1 | u8 | 静态登记版本 |
| 3 | `safety_function_id` | 1 | u8 | 选择该 Profile 内的安全功能契约 |
| 4 | `profile_config_crc32` | 4 | u32 | Profile、功能、必需字段和时序摘要 |
| 8 | `safety_param_crc32` | 4 | u32 | 当前安全参数集摘要 |

传感器只有在以下条件全部满足时才返回 `OK`：

- 当前状态为 `CONTROL_READY`，控制头会话与 HELLO 会话一致。
- 类型、Profile、版本、功能和目录均为静态登记组合。
- `profile_config_crc32` 与本地静态契约一致。
- `safety_param_crc32` 与当前已提交安全参数一致。
- 测量模式、自检、采集链和必需配置允许进入安全运行。

成功响应最后一个 CRC 字节发送完成后，传感器进入 `START_DELAY`。CPU2 完整验证成功响应后进入 `RX_ONLY_ACTIVE`，此后不再发送任何帧。

每个 HELLO 会话只允许一次成功 START。STOP、HELLO 重建、故障或重启后必须使用新会话。

### 12.2 Profile 配置摘要

`profile_config_crc32` 对以下 26 字节规范化序列计算 CRC32C：

| 顺序 | 字段 | 类型 |
| ---: | --- | --- |
| 1 | `protocol_version` | u8 |
| 2 | `sensor_type` | u8 |
| 3 | `profile_id` | u8 |
| 4 | `profile_version` | u8 |
| 5 | `safety_function_id` | u8 |
| 6 | `payload_len` | u8 |
| 7 | `required_field_mask` | u16 |
| 8 | `report_period_ms` | u16 |
| 9 | `min_inter_report_ms` | u16 |
| 10 | `max_inter_report_ms` | u16 |
| 11 | `start_delay_ms` | u16 |
| 12 | `stop_window_ms` | u16 |
| 13 | `silence_confirm_ms` | u16 |
| 14 | `max_data_age_ms` | u16 |
| 15 | `profile_contract_crc32` | u32 |

所有字段小端串联，不包含结构体填充。`profile_contract_crc32` 覆盖字段布局、类型、单位、倍率、范围、状态/诊断映射、质量算法和规范化规则。

### 12.3 Profile 目录摘要

`profile_catalog_crc32` 对以下规范化目录计算：

1. `catalog_version(u16)`。
2. `entry_count(u16)`。
3. 按 `(sensor_type, profile_id, profile_version, safety_function_id)` 升序排列的固定 12 字节条目：

```text
sensor_type(u8)
profile_id(u8)
profile_version(u8)
payload_len(u8)
safety_function_id(u8)
reserved0(u8=0)
required_field_mask(u16)
profile_config_crc32(u32)
```

目录摘要改变时 CPU2 必须拒绝建立安全流，直到双方固件和机器契约重新配套。

## 13. STOP_STREAM

STOP 请求和成功响应均无负载，控制头必须绑定当前 `session_id`。

CPU2 正常停止时：

1. 先撤销当前安全快照。
2. 等待当前帧或帧尝试结束，并确认总线空闲至少 `bus_idle_guard_ms`。
3. 在 Profile 的 `stop_window_ms` 内发送 STOP。
4. 传感器取消后续报告，返回 STOP 响应。
5. 传感器在响应发送完成后使当前会话失效并进入 `INACTIVE_SILENT`。
6. CPU2 验证响应并观察 `silence_confirm_ms`，确认静默后清除活动端点。

若运行帧损坏，CPU2 可以在 UART 空闲持续时间达到 `bus_idle_guard_ms` 后认定本次帧尝试结束，不要求 CRC 正确后才允许执行安全停止。DM4 候选 `bus_idle_guard_ms=5 ms`。

STOP 超时、错误响应或静默期内仍有运行帧时，CPU2 不得启动另一个端点；应使用 HELLO 安全重建、硬件隔离或进入整机安全状态。

## 14. ACTIVE_REPORT_V2

### 14.1 44 字节公共头

| 偏移 | 字段 | 长度 | 类型 | 说明 |
| ---: | --- | ---: | --- | --- |
| 0 | `sof0` | 1 | u8 | 固定 `0xA5` |
| 1 | `sof1` | 1 | u8 | 固定 `0x5A` |
| 2 | `frame_type` | 1 | u8 | 固定 `0xA2` |
| 3 | `frame_len` | 1 | u8 | `44 + payload_len + 4` |
| 4 | `src_id` | 1 | u8 | 唯一活动端点地址 |
| 5 | `dst_id` | 1 | u8 | CPU2 地址 `0x01` |
| 6 | `protocol_version` | 1 | u8 | 固定 `0x02` |
| 7 | `sensor_type` | 1 | u8 | 与 HELLO 和静态端点配置一致 |
| 8 | `profile_id` | 1 | u8 | 与 START 一致 |
| 9 | `profile_version` | 1 | u8 | 与 START 一致 |
| 10 | `safety_function_id` | 1 | u8 | 与 START 一致 |
| 11 | `header_flags` | 1 | u8 | 初版固定 0 |
| 12 | `session_id` | 8 | u64 | 当前 HELLO/START 会话 |
| 20 | `stream_seq` | 4 | u32 | 首帧为 1，每实际发送一帧严格加 1 |
| 24 | `sample_counter` | 4 | u32 | 完整安全快照采样计数 |
| 28 | `data_age_ms` | 2 | u16 | 快照完成到 SOF 发送的年龄 |
| 30 | `reserved0` | 2 | u16 | 固定 0 |
| 32 | `profile_config_crc32` | 4 | u32 | START 时确认的 Profile 配置摘要 |
| 36 | `safety_param_crc32` | 4 | u32 | START 时确认的安全参数摘要 |
| 40 | `safety_status` | 2 | u16 | 公共安全状态 |
| 42 | `safety_diag` | 2 | u16 | 公共安全诊断 |
| 44 | `profile_payload` | N | bytes | 固定 Profile 负载 |
| 44+N | `frame_crc32c` | 4 | u32 | CRC32C |

公共头保留显式源地址、目的地址和传感器类型，用于检测错寻址、错端点和错误安装。`payload_len` 不在线重复发送，由 `frame_len - 48` 计算，并必须与静态 Profile 表一致。

### 14.2 公共状态位

| bit | 名称 | 含义 |
| ---: | --- | --- |
| 0 | `SNAPSHOT_COHERENT` | 所有业务值来自同一快照 |
| 1 | `DATA_VALID` | 当前快照通过传感器端门禁 |
| 2 | `MODE_MATCH` | 当前模式与安全功能契约一致 |
| 3 | `STREAM_ACTIVE` | 当前流由合法 START 建立 |
| 4 | `CONFIG_MATCH` | Profile 和安全参数摘要一致 |
| 5 | `SELF_TEST_OK` | 必需自检通过且未失效 |
| 6 | `MEASURE_STABLE` | 当前测量阶段稳定 |
| 15:7 | 保留 | 初版必须为 0 |

### 14.3 公共诊断位

| bit | 名称 | 含义 |
| ---: | --- | --- |
| 0 | `SAMPLE_STALE` | 发送端判断数据过期 |
| 1 | `SAMPLE_FROZEN` | 采样计数或核心测量冻结 |
| 2 | `LOCAL_TIMING_ERROR` | 本地上报定时或周期错误 |
| 3 | `UNAUTHORIZED_TX_ERROR` | 未授权发送或状态机错误 |
| 4 | `LOW_SIGNAL_QUALITY` | 信号质量低于 Profile 阈值 |
| 5 | `CONFIG_OR_CALIBRATION_INVALID` | 配置或标定摘要无效 |
| 6 | `SELF_TEST_ERROR` | 必需自检失败 |
| 7 | `ACQUISITION_CHAIN_ERROR` | ADC、定时器、激励或采集链错误 |
| 8 | `POWER_INVALID` | 供电诊断异常 |
| 9 | `INTERNAL_COMM_ERROR` | 传感器内部通信错误 |
| 10 | `VALUE_OR_RATE_ERROR` | 数值、变化率或转换越界 |
| 11 | `STREAM_BUFFER_ERROR` | 快照缓存或组帧异常 |
| 12 | `PROFILE_DATA_INVALID` | Profile 通道级诊断 |
| 13 | `MODE_OR_STATE_INVALID` | 模式、阶段或姿态状态异常 |
| 15:14 | 保留 | 初版必须为 0 |

每个 Profile 必须冻结整帧拒绝诊断掩码和“诊断位—有效字段位”影响矩阵。不得由 CPU2 自行猜测哪些字段仍可使用。

## 15. DM4 Profile：DM4_RUNTIME_V1

### 15.1 静态注册

| 项 | 候选值 |
| --- | --- |
| `sensor_type` | `0x01` |
| `profile_id` | `0x01` |
| `profile_version` | `0x01` |
| `payload_len` | 44 字节 |
| `profile_contract_crc32` | 待机器契约生成 |
| 密度/黏度 `safety_function_id` | `0x01`，必需掩码 `0x1FC4` |
| 液位/测水 `safety_function_id` | `0x02`，必需掩码 `0x1C7C` |

DM4 使用 44 字节公共头、44 字节 Profile 负载和 4 字节 CRC，总帧 92 字节。

### 15.2 44 字节负载

| 帧偏移 | 负载偏移 | 字段 | 长度 | 类型 | 单位/说明 |
| ---: | ---: | --- | ---: | --- | --- |
| 44 | 0 | `field_valid_mask` | 2 | u16 | bit0～bit12 对应 DM4 参数 0～12 |
| 46 | 2 | `signal_quality` | 1 | u8 | 0～100，算法和阈值待冻结 |
| 47 | 3 | `reserved0` | 1 | u8 | 固定 0 |
| 48 | 4 | `software_version_x100` | 2 | u16 | 参数 0，版本号乘 100 |
| 50 | 6 | `storage_version_x100` | 2 | u16 | 参数 1，版本号乘 100 |
| 52 | 8 | `dm4_status_raw` | 4 | u32 | 参数 2，原始状态字 |
| 56 | 12 | `magnetic_zero_mv` | 4 | i32 | 参数 3，V ×1000 |
| 60 | 16 | `level_frequency_hz_x1000` | 4 | u32 | 参数 4，Hz ×1000 |
| 64 | 20 | `water_capacitance_pf_x1000` | 4 | u32 | 参数 5，pF ×1000 |
| 68 | 24 | `temperature_c_x100` | 2 | i16 | 参数 6，℃ ×100 |
| 70 | 26 | `density_kg_m3_x100` | 4 | i32 | 参数 7，kg/m³ ×100 |
| 74 | 30 | `dynamic_viscosity_cp_x100` | 4 | i32 | 参数 8，cP ×100 |
| 78 | 34 | `kinematic_viscosity_cst_x100` | 4 | i32 | 参数 9，cSt ×100 |
| 82 | 38 | `supply_voltage_mv` | 2 | u16 | 参数 10，V ×1000 |
| 84 | 40 | `angle_x_deg_x100` | 2 | i16 | 参数 11，° ×100 |
| 86 | 42 | `angle_y_deg_x100` | 2 | i16 | 参数 12，° ×100 |
| 88 | — | `frame_crc32c` | 4 | u32 | 公共帧 CRC32C |

### 15.3 固定点和字段有效性

- Float32 到固定点的舍入、饱和、最小值和最大值必须由 Profile 契约冻结。
- NaN、无穷、溢出或超范围时必须清除对应有效位并置诊断。
- 软件版本和存储版本只辅助一致性检查，不作为安全测量值。
- CPU2 只有在 `(field_valid_mask & required_field_mask) == required_field_mask` 时才可使用对应安全功能。
- 未被当前安全功能要求的字段仍必须按固定布局发送，但不能绕过自身类型、保留值和帧级 CRC 检查。

### 15.4 DM4 状态字

`dm4_status_raw` 保留 DM4 V4.0 参数 2 语义。模式、测量阶段和激励状态必须通过门禁；Bit16 姿态异常、Bit17 测水异常、Bit18 电压正常极性必须映射到公共诊断和字段有效位。

Bit8～Bit10 是非安全协议“读取后清零”的新数据提示，不能作为安全快照唯一证据。V2 使用 `sample_counter`、`field_valid_mask` 和 `safety_status` 表示原子快照。

## 16. DM4 候选静态时序

| 项 | 候选值 | 说明 |
| --- | ---: | --- |
| `report_period_ms` | 500 ms | 相邻 SOF 目标间隔 |
| `min_inter_report_ms` | 450 ms | CPU2 本地时间下限 |
| `max_inter_report_ms` | 550 ms | 超过后当前流失效 |
| `start_delay_ms` | 150 ms | START 响应发送完成到首帧 SOF |
| `stop_window_ms` | 150 ms | 帧尝试结束后 CPU2 发起停止的窗口 |
| `bus_idle_guard_ms` | 5 ms | 发送 STOP/HELLO 前的总线空闲时间 |
| `silence_confirm_ms` | 700 ms | STOP/HELLO 后的静默确认时间 |
| `max_data_age_ms` | 750 ms | 还必须小于安全功能 FTTI |

这些值进入 Profile 契约，不在 START 中动态改变。认证前必须结合晶振误差、任务调度、方向切换、线缆和 FTTI 重新计算。

## 17. 安全参数维护边界

V2 安全运行核心不提供通用参数读写、分页目录或任意批量写。DM4 参数 0～159 的维护仍遵循正式 DM4 参数语义，但必须满足：

1. 只能在无活动安全会话时执行。
2. 安全相关写入使用暂存、范围/权限校验、原子提交、掉电保护和失败回滚。
3. 实际影响安全输出的参数按稳定参数 ID 清单规范化，计算 `safety_param_crc32`。
4. 提交成功后立即使现有会话失效，禁止沿用旧报告或旧 START 结果。
5. CPU2 必须重新 HELLO，并使用新的安全参数摘要执行 START。
6. 维护协议返回的数据不得直接作为 V2 安全测量输入。

参数 65 的“定时发送/交互通信”不得启动 V2 主动上报；只有合法 `START_STREAM` 可以启动安全流。

## 18. 进入、运行和退出流程

### 18.1 上电和恢复清场

1. CPU2 保持接收，监听至少 `silence_confirm_ms`。
2. CPU2 在总线空闲窗口依次向已配置地址发送 HELLO。
3. 任一端点收到 HELLO 后停止旧流并返回新会话。
4. CPU2 核对地址、类型、设备身份、启动计数、目录摘要和会话新鲜度。
5. 未配置或可选端点无响应时，协议层将其记录为不可用；是否允许其它安全功能继续由整机安全需求决定。

### 18.2 启动唯一端点

1. CPU2 确认本地 `active_endpoint_id=NONE`。
2. 选择目标端点和静态 Profile/安全功能。
3. 取得期望 `profile_config_crc32` 和 `safety_param_crc32`。
4. 发送 `START_STREAM`，完整核对成功响应及逐字节回显。
5. CPU2 设置唯一活动端点并进入 `RX_ONLY_ACTIVE`。
6. 传感器等待 `start_delay_ms` 后发送 `stream_seq=1` 的首帧。

首帧必须在 START 响应完成后的 `start_delay_ms + max_inter_report_ms` 内到达，否则 CPU2 撤销当前流并进入恢复。

### 18.3 安全运行

- CPU2 不发送任何帧。
- 上报周期按连续两个帧 SOF 的 CPU2 本地时间计算。
- 每个周期只发送一帧最新完整快照。
- 没有新快照或存在全局安全故障时，传感器进入 `FAULT_SILENT`，不得重发旧快照。
- `stream_seq` 每实际发送一帧加 1；`sample_counter` 必须对应新采样并严格前进。
- 序号在 `0xFFFFFFFF` 前必须停止并建立新会话，不允许在会话内回绕。
- CPU2 不发送 ACK，传感器不因未收到 ACK 而重发。

### 18.4 正常退出和切换

1. CPU2 先撤销当前安全快照并进入 `EXITING_RX_ONLY`。
2. 在帧尝试结束和总线空闲保护后发送 STOP。
3. 传感器停止、响应并使会话失效。
4. CPU2 验证响应并观察静默确认时间。
5. 只有确认静默后才允许 HELLO 和 START 另一个端点。

## 19. CPU2 数据采用门禁

一帧只有在以下条件全部满足时，才可原子更新安全快照：

1. SOF、`frame_type=0xA2`、总长度和静态 Profile 长度正确。
2. CRC32C 正确，所有保留字段和保留位为 0。
3. 源地址等于唯一活动端点，目的地址、协议版本和传感器类型正确。
4. Profile、版本和安全功能与 START 记录一致。
5. 64 位 `session_id` 与当前会话完全一致。
6. `stream_seq` 严格等于上一帧加 1，不重复、回退、跳号或回绕。
7. `sample_counter` 严格前进并满足 Profile 最大允许增量。
8. 相邻帧 SOF 的本地间隔位于允许范围。
9. `profile_config_crc32` 和 `safety_param_crc32` 与 START 记录一致。
10. 必需公共状态位全部为 1，公共诊断满足 Profile 拒绝掩码。
11. Profile 负载、有效掩码、物理范围、变化率和质量满足契约。
12. CPU2 计算的有效年龄未超过 `max_data_age_ms` 和安全功能 FTTI。

```text
effective_age_ms = saturate_u32(data_age_ms + (now_monotonic - rx_complete_time))
```

CPU2 不得只信任发送端年龄。任一门禁失败时不得更新安全快照；可以更新隔离的非安全诊断缓存，但不得采用部分解析结果。

## 20. 错误和恢复

| 场景 | 处理 |
| --- | --- |
| 普通 CRC、截断或字段错误 | 丢弃整帧，不 ACK，不更新快照 |
| 相邻帧间隔超过上限 | 当前流立即失效并进入恢复 |
| `stream_seq` 重复、回退或跳号 | 当前流立即失效，使用 HELLO 重建 |
| `sample_counter` 不前进 | 判定数据冻结，立即撤销快照 |
| 地址、类型、Profile、会话或配置变化 | 立即拒绝并使用 HELLO 重建 |
| 收到非活动端点合法帧 | 互斥不变量破坏，作为总线级故障 |
| 两个源交替、碰撞或持续占线 | 撤销总线全部安全输入，禁止启动新端点 |
| 活动传感器重启 | 传感器默认静默；CPU2 超时并重新 HELLO |
| CPU2 重启 | 先监听，在空闲窗口逐端点 HELLO 清场，再建立新会话 |
| STOP 失败 | 不启动其它端点；HELLO 重建、隔离或进入安全状态 |

普通坏帧被丢弃后，发送端序号仍会前进，下一帧会暴露序号缺口，因此当前流随即失效。该策略偏向确定性和安全完整性，不以通信可用性换取隐式重同步。

持续占线、无法形成控制窗口或无法安全停止端点时，协议恢复能力已经耗尽，必须在 FTTI 内进入整机定义的安全状态或执行独立隔离。

## 21. 9600 bps 时序预算

按 8N1、每字节 10 bit：

| 帧 | 长度 | 纯线时间 |
| --- | ---: | ---: |
| HELLO 请求 | 32 B | 约 33.3 ms |
| HELLO 成功响应 | 44 B | 约 45.8 ms |
| START 请求/响应 | 36 B | 约 37.5 ms |
| STOP 请求/响应 | 24 B | 约 25.0 ms |
| DM4 运行上报 | 92 B | 约 95.8 ms |
| 最大运行上报 | 120 B | 约 125.0 ms |

500 ms 周期下，DM4 上报占线约 19.2%，最大运行帧占线 25.0%。DM4 帧完成后到下一目标 SOF 仍约有 404 ms，可覆盖 5 ms 空闲保护、150 ms 停止窗口和控制帧传输。

## 22. 与 V1、非安全协议和旧 V2 草案的关系

| 对象 | 关系 |
| --- | --- |
| V1 安全协议 | 独立线上版本；V2 不把 V1 运行帧作为安全输入 |
| DM4 非安全 8 字节帧 | 只提供参数、状态和维护语义，不作为 V2 安全运行帧 |
| DM4 参数编号 | V2 DM4 Profile 保留参数 0～12 的业务语义 |
| 旧 V2 草案 | 被本精简候选版替代；旧 52 字节公共头和 CONFIGURE/ARM 流程不再作为实现输入 |
| CPU2/CPU3 `DEVICE_PROTOCOL_VERSION` | 本协议仅位于 CPU2 与传感器之间时不自动提升 |

V2 与 V1 不自动回退。CPU2 必须根据明确的端点配置和协议版本选择客户端，不能在安全会话失败后静默降级为非安全协议。

## 23. SIL2/SC3 设计追踪

| 通信危险 | V2 措施 | 必须验证的证据 |
| --- | --- | --- |
| 数据损坏 | 固定长度、固定布局、保留位、CRC32C | 位翻转、突发错误、截断、拼接 |
| 重复、丢失、乱序 | 事务序号、流序号、严格连续策略 | 重复、跳号、回退、边界和回绕 |
| 错端点和错寻址 | 源/目的地址、类型、设备身份、静态配置 | 错地址、错类型、设备替换、错源发送 |
| 旧会话和重放 | 双端新鲜因子组成的 64 位会话、HELLO 重建 | CPU2/传感器重启、旧帧重放、会话复用 |
| 数据陈旧和冻结 | 本地周期、`data_age_ms`、`sample_counter` | 延迟、冻结、重复值和本地时钟边界 |
| 错配置 | 目录摘要、Profile 配置摘要、安全参数摘要 | 摘要漂移、参数提交、错误功能和错误固件组合 |
| 快照不一致 | 传感器原子快照、状态、诊断、有效位 | 混合采样、半更新、通道故障 |
| 多端点冲突 | 单活动端点门禁、错源即总线故障、静默确认 | 双源、碰撞、持续占线、切换竞态 |
| 系统性复杂度 | 三命令核心、静态 Profile、固定状态机 | 需求追踪、审查、单元测试、覆盖率和独立验证 |

SC3 不是由协议字段自动获得。每个安全机制仍需对应安全需求、双端实现、独立评审、配置管理、验证覆盖和认证证据。

## 24. 最低验证矩阵

### 24.1 机器契约和 golden frame

- 控制头固定 20 字节，运行公共头固定 44 字节。
- HELLO 请求 32 字节、成功响应 44 字节。
- START 请求/成功响应 36 字节，STOP 请求/成功响应 24 字节。
- DM4 运行帧固定 92 字节，最大运行帧 120 字节。
- 所有偏移连续，多字节字段小端，CRC32C 覆盖范围一致。

### 24.2 控制和会话

- HELLO 在所有传感器状态下只产生安全停止和新会话，不会启动上报。
- host nonce 错误回显、零 sensor nonce、重复会话和旧会话控制均拒绝。
- START 错 Profile、错功能、错目录、错配置摘要和错参数摘要均保持静默。
- START 成功响应丢失时 CPU2 不采用后续数据，并通过 HELLO 恢复。
- STOP 正常、超时、错误响应和响应丢失均覆盖。

### 24.3 运行帧

- CRC 错、截断、超长、保留位非 0、错误地址、错误类型和错误 Profile。
- 会话重放、序号重复/跳号/回退/回绕、采样冻结和数据年龄越界。
- 状态、诊断、有效字段、范围、变化率和质量所有边界。
- 非活动端点发送、两个端点交替、碰撞和持续占线。

### 24.4 真实链路和安全证据

- 9600 bps 短线、目标最长线缆、最坏方向切换和时序抖动。
- CPU2 重启、传感器重启、断线、短路、掉电和恢复。
- 长稳、环境、故障注入、FTTI 和残余错误率论证。
- CPU2、传感器端、机器契约、测试和安全需求双向追踪。

## 25. 当前实施状态

本文只完成精简候选协议设计，尚未实现或验证：

- CPU2 V2 控制帧、64 位会话、静态 Profile 表和单活动端点状态机；
- 传感器端 HELLO 安全停止、START、STOP 和本地周期上报；
- DM4 安全快照、固定点编码和正式 Profile 契约摘要；
- 第二功能传感器类型和 Runtime Profile；
- 机器契约、golden frame、主机测试和固件构建；
- 真实 RS485、目标线长、环境、长稳和故障注入；
- SIL2/SC3 认证证据闭环。

在上述内容完成前，只能表述为“按 SIL2 安全通信原则设计的候选协议”，不得表述为“已满足 SIL2/SC3”。
