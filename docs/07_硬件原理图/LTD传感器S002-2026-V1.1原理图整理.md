# LTD 传感器 S002-2026-V1.1 原理图整理

整理日期：2026-06-10；排版更新：2026-06-11

<div class="report-summary">
  <div><span>图纸对象</span><strong>S002-2026 LTD 传感器板</strong></div>
  <div><span>核心链路</span><strong>DDS 激励 + 压电采集 + MCU 计数</strong></div>
  <div><span>密度原理</span><strong>振动式/谐振式测量</strong></div>
  <div><span>CPU2 边界</span><strong>读取协议结果，不直接采模拟量</strong></div>
</div>

## 资料信息

| 项目 | 内容 |
| --- | --- |
| 原始文件 | `00_原始资料/S002-2026-V1.1.pdf` |
| 来源文件 | `C:\Users\admin\Downloads\S002-2026-V1.1.pdf` |
| 图纸文件 | `S002-2026.SchDoc` |
| 图纸日期 | 2026-06-10 |
| PDF 页数 | 1 页 |
| PDF 元数据 | Creator: `Altium Designer Summer 09`; Producer: `llPDFLib 3.x`; 创建时间：2026-06-10 08:00:00 |
| 文件大小 | 690,153 字节 |
| SHA256 | `15498D0092B4F5C9ADA81763977D60E8378A3A301221DFE487BB1C1B3CC7B227` |
| 整理方式 | `pdfinfo`、`pdftotext -layout/-raw` 提取文本，300 dpi 渲染后人工核对关键模块 |

说明：本文用于固件开发、联调和排故时快速理解 LTD 传感器采集驱动板内部链路，不替代 Altium 源文件、PCB、BOM、硬件评审或本安认证资料。原理图中的部分功能名和公式来自图纸注释；与固件关联部分为结合当前 `LTD_MAIN_CPU2` 代码的分析。

## 图纸定位

该 PDF 是单页 A2 原理图，标题区域未填写独立 Title/Number/Revision，但文件名为 `S002-2026.SchDoc`，图中主要分区如下：

| 分区 | 主要内容 |
| --- | --- |
| 主控芯片 | `MSPM0G3507` 64 脚 MCU，负责本板采集、驱动、通信、模式切换和频率计数 |
| 485 | `SN65HVD72` RS485 收发器，信号名 `TXD`、`RXD`、`DE`、`A+`、`B-` |
| SWD 烧写及晶振 | 4 针 SWD、8 MHz 晶振、`JTMS-SWDIO`、`JTCK-SWCLK` |
| 电源及电压检测 | `ADP7102-3.3` 生成 3.3V，`MCP6241T-E` 输出 `POWERTEST` |
| 加速度计 | `ADXL345BCCZ-RL7`，SPI 接口 `SCLK0/MOSI/MISO/CS`，中断 `INT1/INT2` |
| 磁零点 | Hall 接口和 `TLV9061IDBVR` 比较/缓冲，输出 `ZERO`，使能 `ZERO_EN` |
| 温度测量模块 | `ADS1247` 24 位 ADC，`AIN1+`/`AIN1-` 输入，`IOUT1` 激励，SPI/控制信号接 MCU |
| 测水模块 | `FDC2212` 电容数字转换器，I2C 接口 `SCL/SDA`，LC 网络输出 `TWater` |
| 采集--驱动部分 | 双 `AD9832` DDS、`AD8630` 运放、`MAX942` 比较器、`ADG702` 模拟开关、`AD8629` 自激反馈 |
| 液位检测 | `LEVEL` 模拟量经 `AD8630` 比较后形成 `LEVEL_INT` |
| 外部焊盘/接口 | `VCC`、`GND`、`A+`、`B-`、`POWERTEST`、`TWater`、`Aout1/2`、`Ain1/2`、`AIN1+/-` 等单针接口 |

## 核心硬件链路

### 通信与板级控制

| 项目 | 原理图信息 | 固件关联 |
| --- | --- | --- |
| RS485 通信 | `SN65HVD72`，`TXD` 接 DI，`RXD` 接 RO，`DE` 同时接 DE 和 `/RE` 控制收发方向，外部总线为 `A+`、`B-` | 当前 CPU2 通过 USART6 与传感器/无线链路通信，`LTD_MAIN_CPU2.ioc` 中 PC6/PC7 标为 `SENSOR_TXD6`/`SENSOR_RXD6`，波特率 9600 |
| 固件协议 | 图纸未给出上层协议，只有板内 MCU 与 RS485 硬件 | 当前 `ltd_sensor_communication.c` 按固定 8 字节 LTD/V2 协议实现 `R/W/T/D/B` 命令、校验和模式切换 |
| 传感器识别 | 原理图没有直接暴露编号存储器件；编号应由板内 MCU 固件返回 | CPU2 使用 `DSM_V2_Read_SensorID()` 读取参数 `R 0x16` 识别 LTD 传感器 |

### 电源与电压检测

| 信号/器件 | 说明 |
| --- | --- |
| `VCC` | 本板输入电源，进入 `ADP7102-3.3` LDO |
| `3.3V` | 供 MCU、RS485、ADXL345、FDC2212、ADC 数字侧和模拟控制器件使用 |
| `POWERTEST` | 由 `MCP6241T-E` 运放/比较器相关电路生成，接 MCU `PA24/A0_3`，同时作为外部测试焊盘 |
| `PWR LED` | 3.3V 电源指示 |

固件关注点：CPU2 侧只能看到传感器协议返回值或通信状态，不能直接采集该板 `POWERTEST`。如果后续需要远程诊断传感器板电源，应确认传感器固件是否已把 `POWERTEST` 映射到可读参数。

### 模拟计数与频率测量

图纸注释给出的计数逻辑如下：

| 步骤 | 图纸描述 |
| --- | --- |
| 1 | 采集到的传感器信号通过调制后产生类似正弦波信号，接入 `TIMG8_ETR` 作为计时源 |
| 2 | `TIMG8` 计数达到固定值后，由 `TIMG8_C1` 输出 `AM_COUNT` |
| 3 | `TIMG8_C1` 连接 `TIMG12_C0`，`TIMG8` 捕获计数，达到数量后进行计时 |
| 4 | 通过 `TIMG12` 计时和脉冲计数计算频率值 |

固件关联：

| CPU2 读取项 | 当前代码路径 | 硬件来源推断 |
| --- | --- | --- |
| 液位频率 | `DSM_V2_Read_LevelFrequency()` 读取 `R 0x04` | 传感器板 MCU 由 `TIMG8_ETR`/`AM_COUNT` 等计数链路计算 |
| 密度频率 | `DSM_V2_Read_DensityFrequency()` 读取 `R 0x11`/`R 0x12`，CPU2 再按均方值计算频率 | 与 45 度/22.5 度采集波形、均方值或扫频结果有关 |
| 温度/密度 | `DSM_V2_Read_Temperature()`、`DSM_V2_Read_Density()` | 由传感器板内部 ADC/算法输出，CPU2 只做协议读取和修正 |

## 采集--驱动部分

### DDS 驱动

| 器件/信号 | 说明 |
| --- | --- |
| `U2 AD9832BRUZ` | 产生 `Drive_A` |
| `U3 AD9832BRUZ` | 产生 `Drive_B` |
| 共用控制 | `MCLK`、`SCLK`、`SDATA`、`FSELECT` |
| 独立控制 | `FSY1`、`PL0_1`、`PL1_1`；`FSY2`、`PL0_2`、`PL1_2` |
| 相位关系 | 图纸注释：`Drive_A` 相对 `Drive_B` 超前 45 度或 22.5 度 |
| 驱动接口 | `Aout1` 接驱动组压电陶瓷正极，`Aout2` 接驱动组压电陶瓷负极 |

### 采集与选通

| 器件/信号 | 说明 |
| --- | --- |
| `AD8630` | 用于驱动/采集信号放大、差分处理和 `LEVEL` 比较前级 |
| `MAX942` | 比较器，图纸说明其将 `Drive_B` 与 2.5V 比较生成方波控制信号 |
| `JUDGE1/JUDGE2` | `JUDGE1` 与 `Drive_B` 同相并放大到 5V，用于生成 `Select_A`/`Select_B` |
| `ADG702` | 模拟开关，低电平导通；用于选择 A/B 路采集信号 |
| 选通信号 | `Drive_B` 正半周期时 `Select_A` 高、`Select_B` 低，选通 B 信号；反向半周期选通 A 信号 |
| 采集接口 | `AIN2` 接采集组一组正极，`AIN1` 接采集组另一组负极，GND 接采集组剩余正负极 |

图纸注释写明：测得 `ADG702` 输出波形为从波峰到波谷的半周期波形，剩余半周期为 2.5V 直线。

### 自激反馈与液位检测

| 信号/器件 | 说明 |
| --- | --- |
| `SWAP` | 控制 `1-? SPDT Analog Switch`，选择 DDS 输出或反馈输出 |
| `AD8629` | 红框区域标注“模拟驱动部分--自激反馈”，增益标注 `Au = 11.9` |
| `Drive` | 经过模拟开关后送往后级，图纸通断逻辑写明 IN 低电平时 NC 导通、DDS 输出 |
| `LEVEL` | 采集/选通信号处理后的液位模拟量 |
| `LEVEL_INT` | `LEVEL` 经 `AD8630` 比较形成的中断/数字信号，接 MCU |

需要注意：`SWAP` 的业务模式含义需要结合传感器板 MCU 固件确认。对 CPU2 来说，它通常只表现为不同命令或模式下返回的频率、密度、液位结果。

## 密度测量原理

### 通用原理

从现有原理图和 CPU2 代码看，LTD 密度通道更接近振动式/谐振式密度测量，而不是差压式、浮子扭力式或 CPU2 直接采集模拟量计算密度。

振动式密度测量的基本逻辑是：被测结构在液体中振动时，液体会形成附加质量和阻尼；在结构刚度、几何尺寸和安装条件固定时，谐振频率或振荡周期会随被测介质密度变化。公开资料中，Anton Paar 对振荡式密度计给出的通用形式为 `rho = A * tau^2 - B`，其中 `tau` 为振荡周期，`A/B` 为仪表常数；Emerson 的振动叉密度计资料也说明，叉齿以压电方式在固有频率振动，固有频率会随周围液体密度变化。这些资料只能作为振动式测量机理佐证，不代表本 LTD 板的最终标定公式。

| 参考资料 | 可借鉴结论 | 本板使用边界 |
| --- | --- | --- |
| [Anton Paar: U-tube technology in digital laboratory density meters](https://wiki.anton-paar.com/en/u-tube-technology-in-digital-laboratory-density-meters/) | 振荡周期与密度通过仪表常数关联，可用周期平方形式计算密度 | 说明“周期/频率可反推密度”的通用物理基础 |
| [Anton Paar: Density and density measurement](https://wiki.anton-paar.com/us-en/density-and-density-measurement/) | 数字密度计通过激励振荡并测频来计算样品密度 | 说明“激励-测频-计算密度”的仪表链路 |
| [Emerson Micro Motion Fork Density Meters product data sheet](https://www.emerson.com/documents/automation/product-data-sheet-micro-motion-fork-density-meters-en-64588.pdf) | 振动叉以压电方式在固有频率振动，固有频率随周围液体密度变化 | 说明压电振动结构在液体中可用于直接密度测量 |
| [Analog Devices AD9832 data sheet](https://www.analog.com/media/en/technical-documentation/data-sheets/ad9832.pdf) | `AD9832` 集成 NCO、正弦查找表、频率/相位调制和 DAC | 说明本图 `AD9832` 可作为受控正弦激励源 |

### S002-2026 板级链路

结合原理图，密度相关链路可以整理为以下顺序：

<div class="signal-flow">
  <div class="flow-node"><span class="flow-step">1</span><strong>板内 MCU</strong><span>密度模式、扫频控制和采样时序</span></div>
  <div class="flow-node"><span class="flow-step">2</span><strong>双 AD9832 DDS</strong><span>产生 Drive_A / Drive_B 正弦激励</span></div>
  <div class="flow-node"><span class="flow-step">3</span><strong>压电驱动组</strong><span>Aout1 / Aout2 驱动探头振动</span></div>
  <div class="flow-node"><span class="flow-step">4</span><strong>液体中振动探头</strong><span>频率、相位或周期响应随密度变化</span></div>
  <div class="flow-node"><span class="flow-step">5</span><strong>压电采集组</strong><span>Ain1 / Ain2 接收振动响应</span></div>
  <div class="flow-node"><span class="flow-step">6</span><strong>模拟前端</strong><span>AD8630 放大、MAX942 比较</span></div>
  <div class="flow-node"><span class="flow-step">7</span><strong>半周期选通</strong><span>ADG702 按 Drive_B 选通 45 度/22.5 度采样关系</span></div>
  <div class="flow-node"><span class="flow-step">8</span><strong>计数/扫频算法</strong><span>MSPM0G3507 定时器计数和内部算法</span></div>
  <div class="flow-node"><span class="flow-step">9</span><strong>LTD/V2 参数</strong><span>输出密度、温度、扫频均方值</span></div>
  <div class="flow-node"><span class="flow-step">10</span><strong>CPU2 读取</strong><span>USART6 读取、修正、稳定判定、存储/显示</span></div>
</div>

关键点：

1. `U2/U3 AD9832` 分别产生 `Drive_A` 和 `Drive_B`，图纸注释中 `Drive_A` 相对 `Drive_B` 可超前 45 度或 22.5 度。
2. `Aout1/Aout2` 驱动压电陶瓷，`Ain1/Ain2` 接收压电采集信号；密度变化反映到探头振动响应、相位关系、周期或扫频结果中。
3. `MAX942` 根据 `Drive_B` 和 2.5V 比较生成方波控制；`ADG702` 在不同半周期选通 A/B 采集信号，用于得到与相位/周期相关的采样波形。
4. `SWAP` 和 `AD8629` 自激反馈区域说明本板可能支持 DDS 外部激励和反馈自激两类模式，但实际业务切换条件需要传感器板 MCU 固件确认。
5. 板内 `MSPM0G3507` 完成定时器计数、扫频均方值、温度补偿和密度计算；CPU2 不直接控制 `AD9832`，也不直接读取模拟采集波形。

### CPU2 当前读数口径

CPU2 侧密度测量以 LTD/V2 协议为边界，主要代码路径如下：

| 步骤 | 代码/参数 | 当前含义 |
| --- | --- | --- |
| 切密度模式 | `DSM_V2_SwitchToDensityMode()` | 通过 LTD/V2 命令请求传感器板进入密度测量模式 |
| 读温度 | `DSM_V2_Read_Temperature()`，参数 `R 0x06` | 读取传感器板计算/采集后的温度值 |
| 读密度 | `DSM_V2_Read_Density()`，参数 `R 0x07` | 读取传感器板已经计算出的密度值 |
| 读密度频率 | `DSM_V2_Read_DensityFrequency()`，参数 `R 0x11`/`R 0x12` | 读取 45 度和 22.5 度扫频周期平方均值 |
| 频率换算 | `freq_hz = sqrt(1000000000000.0 / v)` | CPU2 用 `R 0x11` 的平方均值换算显示/诊断频率；比例因子和单位需以传感器协议为准 |
| 固定修正 | `densityCorrection`、`temperatureCorrection` | 密度加 `(densityCorrection - 10000) / 10`；温度加 `(temperatureCorrection - 1000) / 10` |
| 稳定判定 | `SinglePoint_ReadSensor()` | 200 ms 采样；频率变化阈值 1 Hz、密度 0.1、温度 0.2 ℃；稳定窗口默认 5 s，可由 `spreadPointHoverTime` 配置 |

因此，当前工程中“密度值”的来源不是 CPU2 根据频率自行套公式算出的结果，而是传感器板返回的 `R 0x07`。CPU2 读取频率更多用于过程显示、稳定判定和异常诊断；真正的密度标定曲线、温度补偿模型、45 度/22.5 度扫频值如何参与最终密度计算，仍属于传感器板 MCU 固件和标定资料范围。

### 对固件和认证资料的影响

1. 文档和软件应把“CPU2 读数边界”和“传感器板内部算法”分开描述，避免把 `R 0x07` 误写成 CPU2 本地计算值。
2. 如要做计量溯源或 SIL/FMEDA 证据，需要补充传感器板固件版本、密度标定公式/系数、温度补偿策略、异常输出策略和扫频值单位定义。
3. `R 0x11`/`R 0x12` 为 45 度/22.5 度扫频均方值，当前 CPU2 只用 `R 0x11` 换算主频率；`R 0x12` 暂以辅助诊断值记录，是否参与传感器端密度算法需确认。
4. 现场排故时，如果密度为 0、频率为 0 或扫频均方值异常，应优先区分“传感器板未稳定/未检测到有效振动”“RS485 通信异常”“传感器内部标定/温度补偿异常”，不要只按 CPU2 运动流程归因。

## 温度、水位、姿态和零点

| 模块 | 关键器件/信号 | 原理图含义 | 当前 CPU2 关联 |
| --- | --- | --- | --- |
| 温度 | `ADS1247`、`AIN1+`、`AIN1-`、`IOUT1`、`REFIN+/-`、`START`、`DRDY` | 通过精密 ADC 和激励电流采集温度相关输入 | `DSM_V2_Read_Temperature()` 读取 LTD/V2 温度参数 `R 0x06` |
| 水位/测水 | `FDC2212`、`SCL/SDA`、`TWater`、LC 网络 | 电容式测水输入 | 当前 `Sensor_ReadWaterCapacitance()` 只对 `DSM_SENSOR` 开启，LTD 传感器读取分支未启用水位电容 |
| 加速度 | `ADXL345BCCZ-RL7`、`SCLK0/MOSI/MISO/CS`、`INT1/INT2` | 板内姿态/倾角测量 | 当前 `Sensor_ReadGyroAngle()` 同样只对 `DSM_SENSOR` 开启；LTD 分支不读姿态角 |
| 磁零点 | Hall 接口、`SI4559ADY`、`TLV9061IDBVR`、`ZERO_EN`、`ZERO` | 通过 Hall/比较电路检测零点 | CPU2 侧未直接读取该板 `ZERO`，是否参与传感器内部算法需确认传感器固件 |

## 外部接口整理

图纸右下角为单针式接口/测试点，能辨认的网络如下：

| 网络 | 图纸标注 | 用途 |
| --- | --- | --- |
| `VCC` | `VCC` | 本板输入电源 |
| `GND` | `GND`、`G` | 电源和信号地 |
| `A+` | `A+` | RS485 A |
| `B-` | `B-` | RS485 B |
| `POWERTEST` | `P` | 板内电源/电压检测测试点 |
| `TWater` | `W` | 测水 LC 网络输出/测试点 |
| `Aout1` | `T1 +` | 驱动组压电陶瓷正极 |
| `Aout2` | `T2 -` | 驱动组压电陶瓷负极 |
| `Ain1` | `R1_ -` | 采集组输入之一 |
| `Ain2` | `R2_ +` | 采集组输入之一 |
| `AIN1+` | `AI1+` | 温度/ADC 差分输入正端 |
| `AIN1-` | `AI1-` | 温度/ADC 差分输入负端 |

## 和当前工程的对应关系

| 工程位置 | 当前作用 | 与本原理图的关系 |
| --- | --- | --- |
| `LTD_MAIN_CPU2/Core/Inc/main.h` | 定义 `SENSOR_ENN`、`SENSOR_TXD6`、`SENSOR_RXD6` | 对应整机主控板给传感器/无线链路供电和 USART6 通信，不直接控制本图中的 MSPM0 引脚 |
| `LTD_MAIN_CPU2/Core/Src/usart.c` | USART6 配置为 9600 8N1，DMA TX/RX | 与本图 `SN65HVD72` RS485 通信入口对应 |
| `LTD_MAIN_CPU2/Services/Sensor/ltd_sensor_communication.c` | 实现 LTD/V2 固定 8 字节协议和 `R/W/T/D/B` 命令 | 本图板内 MCU 是该协议的从机或下游节点 |
| `LTD_MAIN_CPU2/Services/Sensor/sensor.c` | 识别传感器类型、切换液位/密度模式、读取频率/密度/温度 | 本图采集驱动、温度、水位、姿态等板内算法通过协议读数体现 |
| `docs/01_协议与寄存器/CPU2通信与解耦/传感器与无线通信优化检查.md` | 已整理 CPU2 与无线主从机、LTD/V2、DSM 的 UART6 通信链路 | 本文补充 LTD 传感器内部采集驱动硬件依据 |

## 固件开发关注点

1. CPU2 不直接配置本图中的 `AD9832`、`ADS1247`、`FDC2212` 或 `ADXL345`，这些由传感器板 MCU 控制；CPU2 的职责是供电、通信、模式切换、读数、异常归因和结果校准。
2. LTD/V2 的液位频率、密度频率、温度、密度和传感器编号应继续以协议参数为边界维护，避免在 CPU2 里假设板内具体器件寄存器。
3. 当前 CPU2 的水位电容和姿态读取只对 `DSM_SENSOR` 开启；本原理图显示 LTD 板具备 `FDC2212` 和 `ADXL345`，但是否开放到 LTD/V2 协议需要进一步确认。
4. 如果后续需要诊断传感器板内部电源、零点或液位中断，应先确认传感器板固件是否提供 `POWERTEST`、`ZERO`、`LEVEL_INT` 相关状态参数，再规划 CPU2/CPU3 协议显示。
5. `SWAP`、自激反馈、45 度/22.5 度相位和均方值关系会影响密度/频率算法，固件文档应把 CPU2 计算公式与传感器板返回值口径分开描述。

## 待确认项

| 项目 | 说明 |
| --- | --- |
| 板内 MCU 型号 | 渲染图上可辨认为 `MSPM0G3507` 系列，但完整后缀需以 Altium 源文件或 BOM 为准 |
| `SWAP` 模式 | 原理图给出通断逻辑，但业务模式、何时切 DDS/反馈输出需结合传感器板固件确认 |
| LTD 水位/姿态协议 | 图纸已有 `FDC2212` 和 `ADXL345`，当前 CPU2 代码未对 LTD 分支读取水位电容和姿态角；需确认 LTD/V2 是否开放相应参数 |
| `POWERTEST`/`ZERO`/`LEVEL_INT` 状态 | 图纸显示这些信号接入板内 MCU，但 CPU2 侧无直接采集入口；是否能通过协议读取待确认 |
| 传感器板固件版本 | 原理图不包含传感器板固件版本与参数表；若后续要改协议，应同步补齐传感器端协议文档 |
