# 文档库整理候选清单

更新日期：2026-08-03

本清单站在使用者查找和维护的角度，记录下一批文档整理候选项。CPU2/CPU3 子项目文档已并入整机 `docs/`，后续扫描和整理只以 `docs/` 为正式文档源。

## 盘点结论

当前扫描范围：`docs/`。下表是 2026-07-13 运行 `py tools\check_docs.py` 并按扩展名复核得到的快照；后续以检查脚本实时输出为准。

| 项目 | 结果 |
| --- | ---: |
| 文件总数 | 547 |
| Markdown | 275 |
| HTML | 67 |
| PDF | 48 |
| DOCX | 13 |
| XLSX | 7 |
| DAT | 1 |
| ZIP | 1 |
| 其他资源文件 | 135 |
| 缺少 `README.md` 的业务目录 | 0 |
| 无独立 README 的支撑资源目录 | 4（流程导航根目录、CPU2、CPU3 和 SI 中文整理的 `assets/`，仅图片、样式或脚本） |
| Office 临时锁文件 | 0 |

当前文档库的主要问题不是缺业务目录，也不是同名导出件堆积；当前存在少量同名 Markdown+PDF 文件对，统一以 Markdown 为维护正文，PDF 只作为历史导出或外发阅读件且不阻塞文档检查。`assets/` 仅是流程 HTML 的支撑资源目录，不按业务入口补 README。后续重点是让使用者明确“正文改 Markdown、表格看 README、PDF/DOCX/DAT/ZIP 多数是附件、源表、原始记录或外部资料”。

## 已完成的收敛动作

| 范围 | 处理结果 |
| --- | --- |
| CPU2 子项目文档 | 已并入 `docs/00_程序流程导航/CPU2/`、`docs/01_协议与寄存器/CPU2通信与解耦/`、`docs/03_问题分析与整改/`、`docs/07_硬件原理图/00_原始资料/CPU2芯片手册/` 和 `docs/00_构建与版本/故障码/` |
| CPU3 子项目文档 | 已并入 `docs/00_程序流程导航/CPU3/` 和 `docs/00_构建与版本/故障码/` |
| 测试记录旧日期命名 | 当前维护文件已拆入 `问题复测/`、`版本验证/`、`现场记录/`；未建立单独的 `旧格式归档/` 入口，旧名称只在迁移记录和历史版本说明中保留 |
| 故障码表 | 历史分表已收敛：`LTD故障代码统一表.xlsx` 是面向交付的唯一故障项目工作簿；`LTD内部技术与治理资料.xlsx` 保存协议29的130项治理快照、调用点、链路和迁移证据，协议30～34增量待同步 |
| 本地预览展示层 | `docs-site/` 只同步 `docs/`，但不纳入 Git 跟踪；旧 `site/` 工作区副本已删除，只从 Git 历史追溯 |
| 人工查找索引 | 已补 `版本总览.md`、`附件与源文件清单.md`、`流程排查入口.md`、`问题专题索引.md` 和 SIL `资料用途矩阵.md`；这些索引不改变后续 skill 生成版本文档规则 |
| 新增资料落位 | 已补 `新增资料落位检查表.md`，用于新增、移动或重命名前判断业务域、文件类型、索引同步和验证动作；不改变 skill 生成规则 |
| Office/日志附件责任说明 | 已复核 DOCX、XLSX、DAT、ZIP/TXT 所在父目录 README，并在附件总清单记录覆盖情况；附件仍不转换、不批量改名 |
| 旧路径和旧站点引用 | 已复查旧 CPU2/CPU3 docs、旧 `/html/cpu2`、`/html/cpu3`、`8128` 和旧静态站脚本关键词；活文档入口未继续指向旧目录 |
| 同名 PDF 导出件 | 当前存在少量同名 Markdown+PDF 文件对；Markdown 是维护正文，PDF 按历史导出或外发阅读件处理，默认不检查、不自动同步 |
| 入口页瘦身 | 已把 `00_构建与版本/README.md` 的完整版本历史表移交给 `版本总览.md`；已把根 `docs/README.md` 的文件类型/命名细则移交给使用指南、附件清单和治理清单；已把 SIL 外部资料长表压缩为资料范围入口 |

## 判定规则

| 类型 | 维护口径 | 后续处理 |
| --- | --- | --- |
| 同目录同名 Markdown + PDF | Markdown 作为可维护正文，PDF 视为历史导出件或交付阅读件 | 暂不删除、不新增专用归档目录；在父目录 README 标明“以 Markdown 为准” |
| 外部 PDF、芯片手册、标准文档 | 原始资料，不修改正文 | 保留原厂文件名，放在 `00_原始资料/`、`00_外部资料/` 或芯片手册目录 |
| DOCX/XLSX 表格 | 可能是原始确认表、现场交付表或维护表 | 补 Markdown 索引说明用途、责任人和是否仍在维护 |
| HTML 流程页 | 当前作为流程图、结构图或历史阅读页保留 | 放入业务目录并由文档站统一入口跳转 |
| `docs-site/` 生成内容 | 本地预览层，不是业务资料权威来源 | 只改 `docs/` 源文档；`docs-site/` 整体忽略，不提交 |
| 旧 `site/` 与旧静态站脚本 | 已淘汰展示层 | 不恢复、不发布；旧页面需要追溯时看 Git 历史 |

## 保留观察一：同名 PDF 导出件

当前存在少量同名 Markdown+PDF 文件对，具体清单以 `py tools\check_docs.py` 的提示为准。它们不作为优先清理对象：Markdown 是维护正文，PDF 只作为历史导出或外发阅读件，默认不检查、不自动同步。后续若新增 PDF，应先判断是外部原件、图纸、标准资料、芯片手册还是交付导出版；交付导出版必须在对应 README 写清“以 Markdown 为准”。

| 现有 PDF 范围 | 处理建议 |
| --- | --- |
| `docs/00_构建与版本/` | 版本方案或故障治理阅读导出；同名 Markdown 为维护正文 |
| `docs/01_协议与寄存器/` | 协议原件、协议整理导出或安全通信方案导出；原件只读，项目结论维护在 Markdown |
| `docs/03_问题分析与整改/` | 问题分析阅读导出；同名 Markdown 为维护正文 |
| `docs/04_界面与菜单/00_屏幕资料/` | OLED 屏幕原始资料，只读保存 |
| `docs/06_SIL功能安全认证/00_外部资料/` | SIL 标准、ST 官方资料、包内文档和案例资料，只读保存；用途见 SIL 资料矩阵 |
| `docs/07_硬件原理图/00_原始资料/` | 原理图和 CPU2 芯片手册原件，只读保存；整理结论写入硬件目录 Markdown |

## 保留观察二：Office 和表格源文件

这些文件可能是协议、菜单、故障码或现场测试的源表，不建议直接移动或转换。当前已经在 `附件与源文件清单.md` 补了责任速查和父目录说明覆盖情况，后续只需在具体业务变更时同步对应 README。

| 路径 | 建议定位 |
| --- | --- |
| `docs/01_协议与寄存器/DSM寄存器说明V1.225.xlsx` | DSM 寄存器原始表，建议补充 Markdown 摘要和版本口径 |
| `docs/04_界面与菜单/CPU3参数菜单优化方案.docx` | 菜单方案历史源文档，后续以 Markdown 方案为主 |
| `docs/04_界面与菜单/CPU3状态页显示参数矩阵.xlsx` | 状态页矩阵源表，建议保留并在 README 标为维护表 |
| `docs/04_界面与菜单/LNG计量仪屏幕菜单.docx` | 维护版 Word 导出/评审附件；随已确认菜单和适用版本更新，长期实现口径仍以源码和 Markdown 为准 |
| `docs/05_测试记录/现场记录/2026-05-16_瓦锡兰测罐底测试记录.docx` | 现场测试原始记录，保留在现场记录目录 |
| `docs/00_构建与版本/故障码/LTD故障代码统一表.xlsx`、`LTD内部技术与治理资料.xlsx` | 前者保存协议34组合的137个故障项目（135个共享、2个CPU3本机）；后者保存协议29的130项治理快照、非故障状态、CPU3辅助页、调用点和迁移证据，协议30～34增量待同步；共享定义仍以CPU2源码为准 |
| `docs/01_协议与寄存器/CPU2通信与解耦/无线滑环匹配/无线滑环匹配过程.docx` | 无线滑环匹配过程原始记录，保留在专题目录 |

## 保留观察三：没有同名 Markdown 的阅读件

没有同名 Markdown 的 PDF 不应直接当作导出件处理。先按所在业务目录判断其是否为外部原件、硬件资料、屏幕资料、芯片手册或单独交付件；只在需要形成项目结论时补 Markdown 摘要。

| 路径 | 建议处理 |
| --- | --- |
| `docs/04_界面与菜单/00_屏幕资料/HGS128645-Y-EH-LV_SSD1325_OLED_当前液位计屏幕_中文版.pdf` | 保留在屏幕原始资料目录，README 标明是 OLED 资料 |
| `docs/07_硬件原理图/00_原始资料/` 下原理图和芯片手册 PDF | 保留为原件；固件硬件对照和问题结论写入硬件目录 Markdown |

## 下一轮执行顺序

1. 继续整理芯片手册、屏幕资料、标准资料清单，不批量改名 SIL、芯片手册、标准和原厂资料。
2. 如果某个附件开始长期参与维护，在对应业务目录补 Markdown 摘要，写清权威来源、维护责任和更新触发条件。
3. 后续每批整理后复跑旧路径关键词扫描；迁移映射表中的历史旧路径可以保留。
4. 每批整理后抽查 README 长度；2026-07-20检查提示 `00_程序流程导航/README.md`（133 行）、`版本改动与测试/README.md`（81 行）、`01_协议与寄存器/传感器通信协议/DSM传感器协议/README.md`（122 行）和 GD32 功能安全资料 README（62 行）偏长。它们暂不构成结构错误，后续继续增长时优先拆到专题索引或清单页；新增资料先按 `新增资料落位检查表.md` 判断位置。
5. 需要预览时再运行 `docs-site` 同步和构建，构建结果不提交。

## 暂不执行的动作

- 不批量删除 PDF。
- 不把 DOCX/XLSX 自动转换成 Markdown 后覆盖原件。
- 不重命名 SIL、芯片手册、标准和原厂资料。
- 不手工修改或提交 `docs-site/src/content/docs/`、`docs-site/public/archive/` 等本地同步结果；需要更新时运行同步脚本。
