# CPU2 V1.13.0.1 串口测试命令拆分与构建流程整理改动与测试方案

日期：2026-06-11

## 版本变化

- CPU2：`V1.13.0.0 -> V1.13.0.1`
- CPU3：保持 `V1.11.2.0`
- 升级级别：build，原因是 CPU2 串口测试命令代码拆分、构建流程整理和文档资料归档，不新增对外协议能力。

## 协议版本与兼容性

- `DEVICE_PROTOCOL_VERSION` 保持 `7`。
- 不新增 CPU2/CPU3 共享命令码、状态码、寄存器地址或参数字段。
- CPU2 本地串口测试命令字符、参数格式和执行入口保持兼容。
- 本次只把测试命令实现从 `measure.c` 拆到 `test.c`，暂不区分正式/测试构建。
- CPU3 屏幕驱动与刷新资料整理只新增分析文档和屏幕资料归档，不改变 CPU3 固件代码或协议行为。

## 参数存储兼容性

- CPU2 `DEVICE_PARAM_VERSION`：保持 `3`。
- `DeviceParameters` 结构大小：不变。
- 升级是否清参数：否；从 CPU2 `V1.13.0.0` 升级到 `V1.13.0.1` 不会因参数存储版本触发恢复出厂。
- 现场动作：正常升级即可；本次不要求重新下发参数。

## 本次修改

- `measure.c` 保留正式业务串口命令映射和主循环调度，测试串口命令统一转交 `Test_ProcessSerialCommand()`。
- `test.c` 承接 `SC`、`SP*`、`B/BE/BJ/BJP`、`A/C/D/E/F/H/J/L/M/N/T*/Y*/X` 等本地串口测试命令，并增加空命令保护。
- `test.h` 新增 `Test_ProcessSerialCommand()` 声明，避免 `measure.c` 继续保存大量测试实现。
- GitHub Actions CPU2/CPU3 构建流程改为显式 `source_dir`/`build_dir`，使用项目固定构建目录并按 `.elf/.hex/.bin/.map` 上传产物。
- `.gitignore` 忽略 `.superpowers/` 本地工具目录。
- 新增 CPU3 屏幕驱动与刷新问题分析文档，归档当前 HGS128645-Y-EH-LV / SSD1325 OLED 屏资料，并同步问题分析和界面文档索引。

## 影响范围

- CPU2 串口命令解析文件职责边界。
- CPU2 本地测试命令维护入口。
- GitHub Actions CPU2/CPU3 构建和产物上传流程。
- 项目文档中的 CPU3 屏幕资料索引和屏幕刷新整改分析。
- 不影响 CPU2/CPU3 共享协议、参数存储结构、CPU3 固件代码或外部协议适配。

## 重点测试

| 测试点 | 准备条件 | 步骤 | 预期结果 | 通过标准 |
| --- | --- | --- | --- | --- |
| CPU2 编译 | 已配置 `build/LTD_MAIN_CPU2` | 执行 `cmake --build build\LTD_MAIN_CPU2` | CPU2 重新编译并链接成功 | 生成 `LTD_MAIN_CPU2.elf/.hex/.bin` |
| 串口命令结构 | 查看源码 | 检查 `measure.c`、`test.c`、`test.h` | 正式命令留在 `measure.c`，测试命令入口在 `test.c` | `measure.c` 调用 `Test_ProcessSerialCommand()`，`test.c` 不包含 `ProcessMeasureCmd()` |
| 编码安全 | CPU2 源码为 GBK | 按 GBK 读取目标文件 | 无替换字符或 `??` | `measure.c`、`test.c`、`test.h` 均通过 |
| 屏幕资料归档 | PDF 已放入屏幕资料目录 | 计算 SHA256 | 与 README 记录一致 | SHA256 为 `949660D1F522F4D4BC21988B5DD271422138B58B8FC92E5423C7CADCAD004D29` |
| 提交前版本检查 | 目标文件已暂存 | 执行 `py tools\check_version_bumped.py` | CPU2 固件改动包含版本头和日志 | 脚本通过 |

## 回归范围

- CPU2 串口正式业务命令映射和主循环调度。
- CPU2 本地串口测试命令解析。
- CPU2 测量命令入口 `ProcessMeasureCmd()`。
- CPU2/CPU3 CI 构建配置。
- 文档索引中新增屏幕资料路径和问题分析文档路径。

## 已执行验证

- `cmake --build build\LTD_MAIN_CPU2`：通过。
- `git diff --check -- LTD_MAIN_CPU2/Application/Src/measure.c LTD_MAIN_CPU2/Application/Src/test.c LTD_MAIN_CPU2/Application/Inc/test.h`：通过。
- GBK 编码检查：`measure.c`、`test.c`、`test.h` 均未出现替换字符或 `??`。
- 结构检查：`measure.c` 调用 `Test_ProcessSerialCommand()`，`test.c` 提供测试命令入口，`test.c` 不包含 `ProcessMeasureCmd()`。
- `Get-FileHash -Algorithm SHA256 docs\04_界面与菜单\00_屏幕资料\HGS128645-Y-EH-LV_SSD1325_OLED_当前液位计屏幕_中文版.pdf`：与索引记录一致。

## 未验证风险

- 尚未在实物板上重新逐条执行拆分后的串口测试命令；本次主要验证编译、结构和迁移边界。
- GitHub Actions workflow 已做配置审查，但尚未在远端 CI 环境实际跑完。
- CPU3 屏幕驱动整改文档仅为分析和方案，不代表相关代码已经实施。
