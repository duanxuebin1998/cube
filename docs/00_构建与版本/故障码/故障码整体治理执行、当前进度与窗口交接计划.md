# 故障码整体治理执行、当前进度与窗口交接计划

更新日期：2026-08-17（“当前进度”保留2026-07-20交接快照）

> 说明：阶段、构建和台架条目保留2026-07-16协议22治理窗口的交接口径，“当前进度”保留2026-07-20共享协议25交接快照。现行协议34、CPU2/CPU3 V1.40.0.0及当前统计以本目录README和《当前程序故障代码清单》为准。

本文只记录故障码长期优化计划的执行情况、当前进度和后续交接。现行编号、现场说明和历史迁移关系分别以《当前程序故障代码清单》、正式工作簿和内部技术工作簿为准。

## 计划执行情况

| 阶段 | 状态 | 当前结果 |
| --- | --- | --- |
| 第一阶段：资料统一 | 已完成 | 统一正式工作簿、内部技术工作簿、当前清单和目录入口；一代与二代资料分开维护 |
| 第二阶段：分类口径 | 已完成 | 固化责任域、故障层级、处理策略、使用状态和治理意见；二代编号优先沿用“一代类别加10、同义子码不变” |
| 第三阶段：使用链路复查 | 已完成 | 核对CPU2产生与上传、统一日志、CPU3显示和恢复出口；识别仍由一个通用码覆盖多种原因的路径 |
| 第四阶段：重分类、删减和历史归档 | 已完成 | 协议17完成二代责任域重分类；历史页按CPU2程序版本归档，后续版本允许复用相同编号 |
| 第五阶段：多义故障码全部拆分 | 已完成 | 协议19将当前程序能够稳定区分、且现场处理方法不同的原因拆成独立编号；CPU2/CPU3定义和实际产生路径已同步 |
| 第六阶段：代码与资料同步 | 已完成 | 协议22按一代对照和最低可用子码重排；CPU2/CPU3定义、正式工作簿、内部工作簿和当前清单已同步 |
| 第七阶段：软件复验 | 已完成（本轮范围） | 协议22共享编号唯一性、双端一致性、工作簿、相邻契约和双端clean-first构建已复验；CPU3分布结果同步本机码已由对应功能任务归档 |
| 第八阶段：台架验证 | 未执行 | 协议21/22混搭拦截、重编号后双端显示、日志、停机和恢复路径仍需实机验证 |

## 当前进度（2026-07-20 / 共享协议25交接快照）

- CPU2故障码枚举共131项：2项非故障状态、129项共享故障。
- 当前CPU3故障码枚举共133项：与CPU2同步131项，另有2项CPU3本机故障 `20-7 主控单元通信超时`、`20-13 分布结果读取失败`。
- 正式工作簿共131个故障项目：128项使用中、3项保留。
- 3项保留编号为 `12-6 编码器上电变化`、`15-16 实高偏差过大`、`21-5 扭力传感器饱和`；当前程序不会主动报出，启用前仍需补齐调用点和验证。
- 当前共有10个责任域：11电机驱动、12编码器、13传感器与密度、14零点与位置检测、15测量过程、17参数与存储、18模拟输出与自检、20设备通信链路、21扭力检测、22系统与软件。
- CPU2 V1.27.0.0协议21故障表已完整归档；CPU2 V1.25.0.0～V1.26.0.0和V1.24.0.0历史页继续保留。历史页内容不回写，二代后续CPU2版本允许复用相同编号。
- 正式工作簿继续保留一代计量仪历史表；二代当前编号优先与一代建立对照，但责任域或现场处理不同的项目不强行沿用同一子码。
- 协议19的故障码拆分未单独发布，已随协议21组合CPU2 `V1.27.0.0`、CPU3 `V1.25.0.0`发布。协议22同样未单独发布，其重排后的现行编号随协议23组合CPU2 `V1.28.0.0`、CPU3 `V1.26.0.0`首次正式发布。
- CPU2 `DEVICE_PARAM_VERSION` 保持3，`DeviceParameters`结构、大小和CRC范围不变；本次故障码拆分不会清除设备参数。
- 协议22的参数/测量、传感器、CPU3故障原因显示、CPU2通信超时、TMC5130、AD5421、SI、AO和固定点契约均通过；Markdown链接和故障码目标文件`git diff --check`通过。
- CPU2、CPU3 clean-first构建通过：CPU2 ELF为text 306676、data 2328、bss 33992；CPU3 ELF为text 179760、data 37940、bss 80404。
- CPU3本机 `20-13 分布结果读取失败` 已纳入正式表、内部技术表和目录契约；该项不改变129项共享故障的编号、语义和双端一致性，也不提升 `DEVICE_PROTOCOL_VERSION`。
- 文档结构检查已通过，流程导航同步检查因并行功能移除或改名 `CPU2_CommFetchSiProfilePayload` 未通过；该问题不属于本轮故障码重编号范围。
- 参数A/B分区写后读回失败使用 `17-12`；只有当前无其它故障时才置入全局故障状态，避免覆盖正在处理的测量、电机或传感器故障。
- 正式工作簿当前页共131项且无重复，CPU2 V1.27.0.0协议21历史页保持不变；2026-07-20复核的正式工作簿SHA256为`5438CA0A29C839C7D4A884067D3F0271C7808513422B6E8658CE6C0F2460BD83`，内部技术工作簿SHA256为`5E18DF0B3B33EC02E94633226DCA4B1CFD157663834D9DED455A5FAB88A40DDF`。

## 发布与台架交接

1. 协议22未单独发布，协议23首个正式组合已确定为CPU2 V1.28.0.0 / CPU3 V1.26.0.0；`CHANGELOG.md`和详细改动与测试方案已随发布同步，参数存储版本保持3。
2. 协议23固件必须成对烧写。分别验证协议23正常配套，以及协议21/22/23交叉烧写时严格版本门禁能够阻止运行。
3. 对32项重编号故障执行代表性故障注入，至少保存触发条件、屏幕“故障类别-故障码”、串口日志、停机动作、恢复条件和复测结果。
4. 现场查询旧故障时，先确认设备代际和CPU2程序版本；CPU3本机 `20-7` 按CPU3程序版本确认。共享协议版本只用于判断CPU2/CPU3是否配套，不用于选择历史故障码表。

## 交接完成标准

- 源码定义、产生路径、名称/原因映射、CPU3显示、工作簿、当前清单和协议记录数量及编号一致。
- 软件专项检查、字库检查、文档检查和双端clean-first构建均有与最终代码一致的通过记录。
- 正式发布版本、协议23、参数存储兼容结论和版本改动与测试方案一致。
- 台架故障注入和协议混搭验证有可复查证据；未执行项继续明确记录，不以静态检查或构建代替实机结论。
