# 故障码整体治理执行、当前进度与窗口交接计划

更新日期：2026-07-14

本文只记录故障码优化计划的执行情况、当前进度和后续交接，不重复展开分类论证和调用点细节。详细编号、说明和迁移关系分别见当前清单、正式工作簿和内部技术工作簿。

## 计划执行情况

| 阶段 | 状态 | 已完成内容 |
| --- | --- | --- |
| 第一阶段：资料统一 | 已完成 | 正式表、内部表、Markdown入口和两代历史表已统一维护 |
| 第二阶段：分类口径 | 已完成 | 固化责任域、故障层级、处理策略、使用状态和治理意见 |
| 第三阶段：链路复查 | 已完成 | 逐码核对CPU2产生、日志、CPU3显示和恢复出口 |
| 第四阶段：重分类与重编号 | 已完成 | 形成协议17编号；保留3项、停用12项，旧编号不复用 |
| 第五阶段：代码和资料同步 | 已完成 | CPU2/CPU3枚举、日志、显示、AO判断、契约脚本、工作簿和协议资料已同步 |
| 第六阶段：软件验证 | 已完成 | 11项故障码/协议契约、CPU3字库、CPU2/CPU3构建、文档结构、链接和差异检查全部通过 |
| 第七阶段：台架验证 | 未执行 | FI-01～FI-08故障注入和协议16/17交叉烧写需实机完成 |

## 当前结果

- CPU2枚举共65项：2项非故障状态、63项共享故障。
- CPU3同步65项，并新增1项本机故障 `20-7 / 0x00140007`。
- 正式表共64项：使用中61项、保留3项。
- 保留项：12-6编码器上电变化、15-16实高偏差过大、21-5扭力传感器饱和。
- 12项停用码只保存在CPU2 V1.24.0.0历史表；一代计量仪历史表保持独立。
- `DEVICE_PROTOCOL_VERSION` 已由16调整为17；故障寄存器地址、宽度和参数存储版本不变。
- 发布版本已确定为CPU2 `V1.25.0.0`、CPU3 `V1.23.0.0`；参数存储兼容且不会清参，因此按功能级变更提升次版本，不提升软件主版本。
- 软件验证已完成：故障码目录、CPU3本机通信故障、显示原因、传感器、参数/测量、TMC5130、AD5421、安全传感器、LTD、DSM、SI共11项契约通过；CPU3字库缺字为0；CPU2和CPU3构建通过；文档、链接和`git diff --check`通过。
- 本次只调整故障码常量、日志/显示映射和资料，不改变命令入口、测量步骤、状态机或恢复流程，因此程序流程HTML/SVG无需更新。

## 窗口交接

1. 提交CPU2 `V1.25.0.0`、CPU3 `V1.23.0.0`及配套 `CHANGELOG.md`、版本改动与测试方案，并保持其他工作区改动不混入。
2. 使用协议17的CPU2、CPU3成对烧写，验证协议不匹配拦截。
3. 执行FI-01～FI-08台架故障注入，保存屏幕、串口日志、注入条件和恢复结果。
4. 现场查历史故障时先确认设备代际和CPU2程序版本；CPU3本机20-7按CPU3程序版本确认。

## 交接完成标准

- 软件检查和双端构建全部通过。
- 发布版本、协议17、正式表、当前清单和协议记录一致。
- 台架故障注入有可复查证据；未执行项明确记录，不以软件检查代替实机结论。
