# 电机运动当前计划完成审计

更新日期：2026-06-12

## 1. 审计范围

本审计面向当前隔离工作区：

```text
D:\CUBE\.worktrees\motor-motion-structure
```

当前分支：

```text
codex/motor-motion-structure
```

审计目标是确认《电机运动函数简化优化计划.md》和《电机点动与位置模式分层优化方案.md》中，当前环境可以完成的代码、文档和本地验证项是否已经闭环；同时明确必须依赖台架故障注入、产品决策或提交发布动作的项目。

归档说明：原《电机运动函数简化优化计划.md》现为 `docs/02_需求与计划/已实现/2026-06-12_电机运动函数简化优化计划.md`；原《电机运动剩余台架验证执行规程.md》现为 `docs/02_需求与计划/未实现/2026-06-12_电机运动剩余台架验证执行规程.md`；原台架记录现为 `docs/05_测试记录/版本验证/2026-06-11_CPU2_V1.13.0.1_电机运动函数台架验证记录.md`。

## 2. 本地可执行项完成状态

| 项目 | 要求 | 当前证据 | 结论 |
| --- | --- | --- | --- |
| worktree 隔离 | 在独立 worktree 和独立分支执行，不污染主工作区 | `D:\CUBE\.worktrees\motor-motion-structure`，分支 `codex/motor-motion-structure` | 已完成 |
| 目标规划统一 | 正式相对、正式绝对、Jog 相对、Jog 绝对入口共用目标规划 | `MotorMotionTargetPlan`、`MotorMotion_BuildRelativeTargetPlan()`、`MotorMotion_BuildAbsoluteTargetPlan()` 已进入 `motor_ctrl_motion_api.c` | 已完成 |
| 执行策略分离 | 不把正式位置模式和 Jog 速度模式合并成一个大函数 | `MoveToPosition()` 仍走正式位置目标和 `MoveAndWait()` 分段；Jog 仍走 `MotorMotion_JogMoveToTargetInternal()` | 已完成 |
| 运行前门控收敛 | 收敛就绪门控，但保留强制调试和健康检查差异 | `MotorMotion_CheckReadyForProtectedMotion()` 已被正式、Jog、无检测路径按参数复用 | 已完成 |
| 命令切换与启动观察 | 正式距离运动复用命令切换 helper 和启动观察 helper | `MotorMotion_CheckCommandAbortWithSpeedScope()`、`MotorMotion_WaitMoveStartObservation()` 已被 `MotorCtrl_MoveAndWait()` 使用 | 已完成 |
| Jog 单轮运行保护 | 收敛 Jog 循环末尾健康、撞底、丢步、运动状态和超时检查 | `MotorMotion_CheckJogRuntimeGuards()` 已被 `MotorMotion_JogMoveToTargetInternal()` 使用 | 已完成 |
| 第二轮等待循环收敛 | 只抽窄 helper，不合并统一大循环 | `MotorMotion_CheckAbortRefreshAndHealth()`、`MotorMotion_CheckStoppedAndRefresh()` 已被对应等待路径使用 | 已完成 |
| 第三轮策略评估 | 评估是否用 Jog 替换默认绝对位置策略 | 《电机运动第三轮策略评估记录.md》已记录结论：不替换默认 `MoveToPosition()` | 已完成 |
| 结构检查脚本 | 有自动检查四个入口和关键 helper 的脚本 | `tools/check_motor_motion_target_plan.py` 当前工作区通过，`--git-ref HEAD` 可抓到基线缺失项 | 已完成 |
| 文档同步 | README、HTML、计划、分层方案和评估记录能反映当前状态 | 本目录 README 已索引当前计划、分层方案、第三轮评估和本审计记录 | 已完成 |

## 3. 本地验证状态

已执行或本轮必须执行的本地验证项：

| 验证项 | 命令或方法 | 通过标准 |
| --- | --- | --- |
| 结构脚本 | `py tools\check_motor_motion_target_plan.py` | 当前工作区通过 |
| 红灯验证 | `py tools\check_motor_motion_target_plan.py --git-ref HEAD` | 基线失败，并列出缺失 helper 和调用点 |
| 编码检查 | `motor_ctrl_motion_api.c` 按 GBK/936；文档和脚本按 UTF-8 | 无 `U+FFFD`、无异常替换字符 |
| 冲突标记检查 | `rg --line-number "^(<<<<<<<|=======|>>>>>>>)" LTD_MAIN_CPU2 tools` | 无输出 |
| C 注释规则检查 | `rg --line-number "//" LTD_MAIN_CPU2\Services\MotorControl\motor_ctrl_motion_api.c` | 无输出 |
| diff 检查 | `git diff --check` | 退出码 0；允许 Git 的 LF/CRLF 提示 |
| CPU2 构建 | `cmake --build build\LTD_MAIN_CPU2` | 退出码 0 |

## 4. 未在当前环境完成的项目

以下项目不是普通本地编辑可以完成的事项，不应在文档中写成已通过：

| 项目 | 未完成原因 | 当前处理 |
| --- | --- | --- |
| T15 编码器首帧缺失 | 需要人工断开、屏蔽或模拟编码器首帧缺失 | 保持未覆盖风险，发布级验证前补做 |
| T16 TMC/24V 故障 | 需要人工制造 SPI、驱动或 24V 异常 | 保持未覆盖风险，发布级验证前补做 |
| 低于 `200mm` 台架下限 | 当前现场限制是不低于 `200mm`，未实物下发低位目标 | 记录为台架限制，是否固化为固件边界需产品/现场决策 |
| 正式位置模式与 Jog A/B 对比 | 当前只有有限速度和小行程数据 | 行为替换前必须补同目标、同速度、同起点对比 |
| 发布准备和升版 | 计划要求只有用户明确提交、发布、升版或合并时才执行 | 当前不暂存、不提交、不升版 |

上述外部项的现场执行步骤已整理到《电机运动剩余台架验证执行规程.md》。该规程只提供人工台架步骤和记录格式，不把未实测项改写为通过。

## 5. 版本和协议影响

当前 `motor-motion-structure` worktree 的本轮结构优化：

- 未修改 CPU2/CPU3 共享协议。
- 未新增 Modbus 寄存器。
- 未修改参数存储布局。
- 未修改公开 C 接口签名。
- 未把默认 `MotorCtrl_MoveToPosition()` 替换为 Jog 策略。

提交或发布前仍需按最终 diff 重新判断 CPU2 固件版本影响。本审计不替代提交流程中的版本检查。

## 6. 当前计划退出结论

当前计划的本地可执行部分已经收敛到以下状态：

- 第一轮结构优化已有历史本地验证和 T01-T14 台架记录。
- 第二轮等待状态机窄 helper 收敛已经完成。
- 第三轮策略评估已经完成，结论是不进入默认策略替换。
- 第五轮文档同步已覆盖当前代码结构、计划、分层方案、策略评估和本审计记录。
- 剩余外部验证项已整理为《电机运动剩余台架验证执行规程.md》。
- 第六轮发布准备未执行，因为当前未进入暂存、提交、发布、升版或合并流程。

后续如果继续推进，下一步不应再扩大结构重构，而应按《电机运动剩余台架验证执行规程.md》补 T15/T16 故障注入、`200mm` 下限策略决策和正式/Jog A/B 数据；只有这些证据完成后，才进入会改变默认运动策略的代码实验。

## 7. 本轮实际验证结果

本轮已在 `D:\CUBE\.worktrees\motor-motion-structure` 执行以下验证：

| 验证项 | 命令或方法 | 结果 |
| --- | --- | --- |
| 当前结构检查 | `py tools\check_motor_motion_target_plan.py` | 通过，输出 `Motor motion target plan check passed.` |
| 基线红灯检查 | `py tools\check_motor_motion_target_plan.py --git-ref HEAD` | 按预期失败，列出基线缺少目标规划、门控、启动观察、Jog 运行保护、等待循环和停稳收尾 helper |
| 编码检查 | GBK/936 读取 `motor_ctrl_motion_api.c`，UTF-8 读取本目录文档和脚本 | 通过，未发现 `U+FFFD` 或 GBK 源码中的 `??` 替换 |
| 冲突标记检查 | `rg --line-number "^(<<<<<<<|=======|>>>>>>>)" LTD_MAIN_CPU2 tools` | 无输出 |
| C 注释规则检查 | `rg --line-number "//" LTD_MAIN_CPU2\Services\MotorControl\motor_ctrl_motion_api.c` | 无输出 |
| diff 检查 | `git diff --check` | 通过，仅有 Git 提示文档 LF 后续会按 CRLF 处理 |
| CPU2 构建 | `cmake --build build\LTD_MAIN_CPU2` | 通过，输出 `ninja: no work to do.` |

当前工作区未暂存、未提交；未执行新的台架测试。
